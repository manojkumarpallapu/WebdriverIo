// For this i have installed the geckodriver.exe 
// Then included the firefox browswer in the wdio.conf.js.now, as the chrome driver is already installed 
// This will automatically run in the chrome driver.
//But for the firefox we need run this command./ geckodriver.exe.
//Then it will run simultaneously in the both browsers.this is the cross browswe testing